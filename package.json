{
  "name": "dcab-bim-report-studio",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down"
  }
}
